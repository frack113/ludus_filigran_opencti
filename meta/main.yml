---
dependencies:
  - role: geerlingguy.docker

galaxy_info:
  role_name: ludus_filigran_opencti
  author: frack113
  description: An Ansible Role that install Filigran OpenCTI and OpenBAS on Linux
  company: freelance
  license: GPLv3
  min_ansible_version: "2.10"
  platforms:
    # Modify the platforms here as required
    # This example includes a lot, see https://github.com/ansible/ansible-lint/blob/main/src/ansiblelint/schemas/meta.json for all possibilities
    - name: Debian
    - name: Ubuntu
  galaxy_tags:
    - ludus
    - opencti
    - purple

collections: [community.docker] # Add any collections needed here
